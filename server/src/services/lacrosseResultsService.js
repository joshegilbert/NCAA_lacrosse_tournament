/**
 * Future: sync winners from an external NCAA / stats API.
 * Not implemented in v1 — manual admin entry only.
 */
async function syncResults() {
  throw new Error('lacrosseResultsService.syncResults is not implemented')
}

module.exports = { syncResults }
